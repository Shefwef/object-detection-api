# Vercel / npm 10 defaults to strict peer-dep resolution.
# React 19 stable is very new — some transitive deps still declare
# peerDependencies.react="^18". Enabling legacy-peer-deps keeps the
# install working while the ecosystem catches up. Removing this later
# is safe once every dep in the tree lists React 19 support.
legacy-peer-deps=true
